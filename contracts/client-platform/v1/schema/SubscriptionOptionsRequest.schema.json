{
  "additionalProperties": false,
  "properties": {
    "operation": {
      "enum": [
        "reference",
        "client_id_save",
        "client_id_reset"
      ],
      "title": "Operation",
      "type": "string"
    },
    "provider_id": {
      "enum": [
        "codex",
        "claude_subscription",
        "xai_oauth"
      ],
      "title": "Provider Id",
      "type": "string"
    },
    "provider_revision": {
      "pattern": "^[0-9a-f]{64}$",
      "title": "Provider Revision",
      "type": "string"
    },
    "value": {
      "anyOf": [
        {
          "maxLength": 512,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Value"
    }
  },
  "required": [
    "provider_id",
    "provider_revision",
    "operation",
    "value"
  ],
  "title": "SubscriptionOptionsRequest",
  "type": "object"
}

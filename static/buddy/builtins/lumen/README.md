# Buddy Lumen Pack

This is a bundled Buddy generated-motion pack. It ships a prompt-generated preview image and six curated image-to-video state loops that are played locally by Row-Bot's canvas runtime.

Required clips: `idle`, `thinking`, `working`, `approval`, `success`, and `error`.

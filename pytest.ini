[pytest]
testpaths = tests
addopts = --ignore=tests/test_output_log.txt --basetemp=.tmp/pytest_tmp
cache_dir = .tmp/pytest_cache
markers =
    live_provider: opt-in live provider matrix that makes real configured-provider calls

# Real UI Docs Review Status

- Public website under `docs/` remains unchanged.
- Screenshots are captured from the real Row-Bot app route.
- Every screenshot is marked `needs-review` until human visual review approves, replaces, crops, or redacts it.
- Deferred screenshots and follow-up selectors are listed in `docs-build/reports/docs-real-ui-review.md`.
- CI remains validation-only and does not publish `docs-site/build`.
